#include "OpenGLCanvas.h"

namespace sevenide::graphics2d::opengl
{

void OpenGLCanvas::drawRectangle(ICanvas::number x,
                                 ICanvas::number y,
                                 ICanvas::number width,
                                 ICanvas::number height)
{
}

void OpenGLCanvas::drawRoundedRectangle(ICanvas::number x,
                                        ICanvas::number y,
                                        ICanvas::number width,
                                        ICanvas::number height)
{
}

void OpenGLCanvas::drawEllipse(ICanvas::number x,
                               ICanvas::number y,
                               ICanvas::number verticalRadius,
                               ICanvas::number horizontalRadius)
{
}

void OpenGLCanvas::drawCircle(ICanvas::number x, ICanvas::number y, ICanvas::number radius)
{
}

void OpenGLCanvas::drawText(ICanvas::number x, ICanvas::number y, const std::string& text)
{
}

void OpenGLCanvas::drawLine(ICanvas::number x1,
                            ICanvas::number y1,
                            ICanvas::number x2,
                            ICanvas::number y2)
{
}

void OpenGLCanvas::drawImage(ICanvas::number x, ICanvas::number y, const IImage& image)
{
}

} // namespace sevenide::graphics2d::opengl